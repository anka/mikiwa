- content_for :title, "#{@group.name} bearbeiten · Mikiwa"

.mw-page-header
  %h1.mw-page-header__title= "#{@group.name} bearbeiten"

.mw-card{ style: "margin-top: 1.5rem; max-width: 520px;" }
  = render "form", group: @group
