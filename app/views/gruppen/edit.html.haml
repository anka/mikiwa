- content_for :title, "#{@gruppe.name} bearbeiten · Mikiwa"

.mw-page-header
  %h1.mw-page-header__title= "#{@gruppe.name} bearbeiten"

.mw-card{ style: "margin-top: 1.5rem; max-width: 520px;" }
  = render "form", gruppe: @gruppe
